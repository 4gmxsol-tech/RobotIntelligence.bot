# Server-side environment variables
# Never commit real credentials.
# Apollo is disabled by default. Leave these unset unless a permitted plan/connector is explicitly enabled.
APOLLO_API_KEY=
APOLLO_ENABLE_FREE_SEARCH=false
APOLLO_ENABLE_ENRICHMENT=false
